# Copie para `.env` e preencha (`.env` nunca é commitado).
# No deploy (Vercel), cadastre as mesmas variáveis em Settings → Environment Variables.
# A chave anon é pública por design — a proteção real é o JWT + RLS no Supabase.
VITE_SUPABASE_URL=https://sua-org.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-aqui
